# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**SolSGE** — *Sol, Sistema de Gestión Empresarial*. An Oracle APEX 24.2 business-management application (App ID **100**, alias `f100`) running on Oracle Autonomous Database. UI strings, page names, columns, and PL/SQL comments are in Spanish — preserve that language when editing.

**Planes activos:**
- `PLAN_VENTAS.md` — módulo Presupuestos/Ventas (F1–F7 cerradas; deuda P30/P31 + P5 menú).
- `PLAN_FACTURACION.md` — módulo Facturación + Caja. **F8 cerrada el 2026-06-07** (tag `f8-facturacion-contado`, commit `a67ccf3`). **F9 pendiente** (cobro de cuotas + recibo).
- `PLAN_KUDE_DOCUMENTOS.md` — representación gráfica impresa de factura (P96) y recibo (P119). **F12+F13 cerradas** (commit KuDE, verificado por PO 2026-06-14).
- **F12 KuDE (2026-06-10):** P96 (Documento Factura) remaquetada al layout **KuDE** de la SET. Su HTML lo genera `WKSP_WORKPLACE.FN_KUDE_FACTURA_HTML(:P96_ID_COMPROBANTE)` (no inline en la página) y usa `FN_NUMERO_A_LETRAS` para el total en letras — ambas en `db/F12_kude_factura.sql`. **Sin CDC ni QR** (no integrado a SIFEN): es representación gráfica con leyenda "sin validez fiscal". Subtotales por tasa se calculan desde `DETALLE_COMPROBANTE.PORCENTAJE_IVA` (las columnas `COMPROBANTES.TOTAL_GRAVADA_*`/`TOTAL_EXENTA` están NULL en los datos reales; solo `TOTAL_IVA_5/10` se llenan). Emisor en `PARAMETROS` TIPO=`EMPRESA`.
- **F13 Recibo (2026-06-11):** P119 (Documento Recibo) recibió la **misma identidad visual** que P96 vía `WKSP_WORKPLACE.FN_KUDE_RECIBO_HTML(:P119_ID_RECIBO)` (`db/F13_kude_recibo.sql`). **Ojo:** el recibo de cobro **NO es un Documento Electrónico SIFEN** (SIFEN solo cubre factura/NC/ND/remisión/autofactura) → se titula "Recibo de Dinero", sin CDC/QR/"KuDE", reusa el estilo solo por coherencia. **Inconsistencia de datos a recordar:** `MOVIMIENTOS_CAJA.MONEDA` guarda el **texto** (`'PYG'`) mientras `COMPROBANTES.MONEDA` guarda el **código** (`'1'`); por eso el join a `MONEDAS` en ambas funciones es `ON (CODIGO_MONEDA = x OR DESCRIPCION = x)`.
- `PLAN_NOTA_CREDITO.md` — módulo **Nota de Crédito Electrónica (F14)**, camino fiscal para revertir/ajustar una factura **fuera** de las 48 h de la anulación (F11). Cerrada el 2026-06-21 (`db/F14_nota_credito.sql`). **F14 NC (2026-06):** backend en `db/F14_nota_credito.sql` — columnas `COMPROBANTES.COD_MOTIVO` (FK `MOTIVOS_NOTA_CREDITO`) + `ID_COMPROBANTE_ORIGEN` (FK a la factura, vínculo autoritativo; el nro impreso se deriva por join, NO se usa `ID_FAC_ORIGEN` en NC nuevas); tablas staging `SOLICITUDES_NOTA_CREDITO` + `SOLICITUD_NC_DETALLE` (workflow con aprobación, el nro NC se reserva recién al aprobar); procs `PRC_SOLICITAR/APROBAR/RECHAZAR_NOTA_CREDITO`, `PRC_VALIDAR_SOLICITUD_NC`, `FN_NC_ELEGIBLE`/`FN_NC_AVISO`/`FN_CANT_ACREDITABLE`, KuDE `FN_KUDE_NOTA_CREDITO_HTML`, vistas `V_SOLICITUDES_NC`/`V_NOTAS_CREDITO`. **La NC NO anula la factura** (queda `ESTADO='A'`); es documento nuevo `TIPO_COMPROBANTE='NC'`. Efectos al aprobar: **stock** ENTRADA solo si motivo Devolución (1,2); **caja** EGRESO solo si factura contado (caja abierta del aprobador, **del día**); **CxC** si crédito (total→anula cuotas; parcial→absorbe desde la última cuota, mantiene `SALDO=Σ cuotas vigentes`). Modelo de entrada por **motivo**: Devolución acredita valor completo (solo cantidad); Descuento/Ajuste usa "Precio Nuevo" y acredita la diferencia. Pantallas APEX: **P124** (lista IR), **P125** (solicitar, IG editable parcial), **P126** (aprobar/rechazar + desglose), **P127** (KuDE NC); link en P66 + entry de menú. Errores `-20970..-20990`. Limitación conocida: el interés financiero del monto acreditado en crédito (parcial) no se re-amortiza. **F14.2 (2026-06-28):** la NC **total** ahora también reversa el **interés de financiación** de la factura (`COMPROBANTES.INTERES_FINANCIACION`, espejo F16/F12) — se suma a `v_total`/`TOTAL_IVA_10` y se guarda en la cabecera de la NC; el KuDE NC muestra fila "Reverso de interés de financiación"; P126 lo suma al desglose. Guard: solo `TIPO_NC='T'` y **primera** NC sobre la factura (sin NC previa aprobada). NC parcial y contado intactos. Antes la NC total documentaba solo los bienes mientras la CxC se anulaba completa (gap doc↔CxC↔IVA). Cambio in-place en `db/F14_nota_credito.sql` (re-correr F14) + P126.
- `PLAN_CIERRE_CAJA.md` — **Cierre y Arqueo de Caja (F17)**, cierra el módulo de Caja de F8. Cerrada el 2026-06-26 (validada por el PO cerrando la caja 121). **Fix de dinero crítico:** `CERRAR_CAJA` v3 (`db/F17_cierre_caja.sql`) ya **no** une `MOVIMIENTOS_CAJA.MONEDA = CAJA_MONEDAS.MONEDA` con igualdad estricta — esa unión ignoraba los `COBRO_CXC`/`EGRESO` que guardan `'PYG'` (texto) mientras `CAJA_MONEDAS` guarda `'1'` (código), subvaluando el cierre (la caja 67 cerraba en su apertura ignorando ~1,7 M). Ahora calcula desde la vista **`V_CAJA_SALDO`** (join normalizado vía `MONEDAS` con `(CODIGO_MONEDA=x OR DESCRIPCION=x)`, mismo criterio que las funciones KuDE). Se recalcularon los cierres históricos mal calculados guardando el valor previo en `CAJA_MONEDAS.MONTO_CIERRE_PREV`. **Arqueo con conteo declarado:** columnas `CAJA_MONEDAS.MONTO_DECLARADO`/`MONTO_DIFERENCIA`; **P61** captura el efectivo contado (`P61_MONTO_DECLARADO`, parsear con `TO_NUMBER(REPLACE(REPLACE(x,'.',''),',',''))` como P67) y muestra el saldo esperado, `CERRAR_CAJA` v3 calcula `diferencia = declarado − esperado`. **Pantallas:** **P62** (Estado de Caja: selector de caja, saldo por moneda desde `V_CAJA_SALDO`, movimientos en vivo con auto-refresh 20s + filtro por tipo, botones Cerrar/Ver arqueo — Classic Reports, no Cards); **P132** (Documento de Arqueo, modal de impresión vía `FN_CIERRE_CAJA_HTML` en `db/F17_1_arqueo_html.sql`, **NO es DE SIFEN** — control interno sin CDC/QR, reusa el estilo `kude` de P96/P119). Errores `-20940..-20941`. Menú "Estado de Caja"→P62 lo agrega el PO en el Builder. Fuera de alcance: multi-moneda real (todo PYG en datos).
- `PLAN_INTERES_FINANCIACION.md` — **Interés de financiación en la factura (F16)**, corrige una inconsistencia fiscal: antes el interés de la venta a crédito se sumaba a la CxC/cuotas pero **no a la factura** (factura < CxC). Cerrada el 2026-06-25. **Opción B (interés en cabecera), scope minimal.** (1) `db/F16_interes_financiacion.sql`: columna `COMPROBANTES.INTERES_FINANCIACION` (monto IVA incluido; NULL en contado) + `TRG_INS_CUENTAS_COBRAR` **v2** — ya **no** recalcula interés (antes `TOTAL*tasa/100`), arma la CxC desde `:NEW.TOTAL_MONEDA_LOCAL` ya financiado; cuotas en **enteros PYG** (`ROUND(...,0)`, SIFEN) con la **última absorbiendo el remanente** → `factura == CxC.SALDO == Σ cuotas` exacto. **No toca ningún trigger de stock** (ventaja de B; el interés vive en cabecera, el detalle queda solo con bienes). (2) **P67**: ítem oculto `P67_INTERES_FINANCIACION` (source = columna, mapeado al form DML) + proceso AFTER_SUBMIT `Calcular interes financiacion` (seq 9, antes del form DML seq 10) que cuando `FORMA_PAGO='1'` calcula `interes=ROUND(base*tasa/100,0)` y financia `TOTAL_MONEDA_LOCAL`/`TOTAL_IVA_10`/`TOTAL_IVA` (IVA del interés `=interes*10/110`). **Ojo:** los ítems de totales de P67 guardan **string formateado** (`1.050.000`) → parsear con `TO_NUMBER(REPLACE(REPLACE(x,'.',''),',',''))`. (3) KuDE: el cambio vive en **`db/F12_kude_factura.sql`** (`FN_KUDE_FACTURA_HTML`, hogar de la función) — fila "Interés de financiación" (monto en columna IVA 10%) + interés sumado al subtotal gravada-10 (reconcilia tabla vs. Total a Pagar) + bloque "Condición de Venta: Crédito" con las cuotas (estilo `gPagCred`). **Footgun de despliegue:** F16 completo requiere re-correr **F16 _y_ F12**. Errores `-20953..-20969`. Fuera de alcance (§6): poblar `TOTAL_GRAVADA_*`/`TOTAL_EXENTA`, fix `DETALLE_COMPROBANTE.ID_TIPO_IVA` (NULL), ~~NC sobre la porción de interés~~ (resuelto para NC **total** en F14.2, ver `PLAN_NOTA_CREDITO.md §12`; NC parcial sigue sin re-amortizar), integración SIFEN real. Validado e2e con la factura `001-001-0000032`.
- `PLAN_REPORTES_COBROS.md` — **Reportes Gerenciales de Cobros/Caja (F22)**, segundo módulo gerencial, replica la plantilla de Ventas (F18). **Cerrado el 2026-06-29.** Errores `-20904..-20909`. Alcance: **Cobranzas + Cartera CxC** (sin flujo de caja). **GOTCHA CRÍTICO:** `MOVIMIENTOS_CAJA.ESTADO` **NO es activo/anulado → es abierta/cerrada de CAJA** (`'A'`=caja abierta, `'C'`=cerrada; al cerrar la caja en F17 los movimientos pasan de A a C). Ambos son dinero válido → **filtrar `ESTADO IN ('A','C')`, nunca solo `='C'`** (eso borra la cobranza de cajas abiertas). **REGLA DE ORO:** recaudación neta = `COBRO_CXC` (A,C) − `EGRESO` de reverso; el reverso (F15, hoy **oculto** vía `PARAMETROS.REVERSO_COBRO_ACTIVO='N'` pero histórico persiste) NO marca el movimiento, lo compensa un EGRESO con `ID_MOVIMIENTO_REVERSADO`. **Habilitador:** tabla `METAS_COBRANZA` (sucursal × mes, `PERIODO DATE`) — `db/F22_metas_cobranza.sql`; **NO** hace falta habilitador de dimensión (el cobrador ya existe en `MOVIMIENTOS_CAJA.USUARIO`). **Vistas** `V_COBROS_MOV`/`V_COBROS_REVERSO`/`V_COBROS_NETO_MES`/`V_COBROS_MEDIO`/`V_CARTERA_CXC`/`V_COBROS_OFICINA_META` (`db/F22_1_vistas_cobros.sql`) = único source of truth. Metas demo: `db/F22_seed_metas_cobranza_demo.sql`. **Dimensiones:** oficina vía `CAJAS.ID_OFICINA`→`OFICINAS.CODIGO_OFICINA` (ojo: PK es `CODIGO_OFICINA`, no `ID_OFICINA`); cobrador=`USUARIO`; cliente vía `PERSONAS`; medio vía `DETALLE_MOVIMIENTO_CAJA.ID_METODO_PAGO`→`METODOS_PAGO`. **Páginas:** **P136** Dashboard de Cobros (KPIs + 6 JET charts: neta/mes, por sucursal, medios donut, **aging de cartera**, **top deudores**, sucursal-vs-meta + selector de mes); **P137** "Generador de Informe de Cobros" (imprimible por filtros: rango de fechas + sucursal + cobrador → `FN_INFORME_COBROS_HTML` en `db/F22_2_informe_cobros_html.sql`, barras CSS + `@media print`, estilo `kude`; medio y aging van como secciones). **P138** "Metas de Cobranza" (Interactive Report) + **P139** "Meta de Cobranza" (form modal) = ABM de metas. **Add-on F18:** se agregó el ABM de `METAS_VENTA` que faltaba: **P140** "Metas de Venta" (IR) + **P141** "Meta de Venta" (modal con select Tipo Vendedor/Sucursal que resuelve la regla 1-de-2). Todas las páginas se importan **aisladas** (`install_p136/137/138/140.sql`); el menú lo agrega el PO. Memoria: [[movimientos-caja-estado-ac]].
- `PLAN_REPORTES_GERENCIALES.md` — **Reportes Gerenciales (F18)**, arranca por **Ventas** como plantilla replicable a Compras/Inventario/Cobros. **Cerrado el 2026-06-28** (validado por el PO). Errores `-20991..-20999`. **Habilitador "vendedor":** `ORDENES_VENTA.USUARIO_CREACION` (estampada por trigger `TRG_OV_USUARIO_CREACION` con `V('APP_USER')`, NO se toca P54) + tabla `METAS_VENTA` (vendedor **o** sucursal × mes, `PERIODO DATE`) — `db/F18_habilitador_vendedor.sql`. Backfill de vendedor/metas es **demo** (`db/F18_seed_vendedor_demo.sql`, `db/F18_seed_metas_demo.sql`). **Vistas** `V_VENTAS_*` (`db/F18_1_vistas_ventas.sql`) = único source of truth. **REGLA DE ORO:** ventas/meta cuentan solo lo facturado, atribuido por la **factura** (`COMPROBANTES.ID_ORDEN_VENTA`), NO por `ORDENES_VENTA.ESTADO`; NC se resta por dos saltos (`ID_COMPROBANTE_ORIGEN`→factura→orden). **P133** Dashboard interactivo (KPIs + 6 JET charts + selector de Mes); **P135** "Generador de Informe de Ventas" (imprimible **por filtros**: rango de fechas día/mes/año/rango + vendedor + sucursal + condición → `FN_INFORME_VENTAS_HTML` en `db/F18_2_informe_ventas_html.sql`, barras CSS + `@media print`, estilo `kude`). El botón del dashboard abre P135. **Gotchas APEX (memoria):** primer JET chart del proyecto (`p_value_format_scaling` rompe el check `JET_CHARTS_SCALING`); región que sostiene ítems debe ser contenedor plano (sin `NATIVE_STATIC_CONTENT` → si no, `ORA-01403` al render); `p_static_id` no va en `create_page_plug`; `unistr` en SQL de un LOV → `ORA-12704`.
- `PLAN_REPORTES_INVENTARIO.md` — **Reportes Gerenciales de Inventario (F23)**, tercer módulo gerencial, replica la plantilla de Ventas (F18)/Cobros (F22). Errores `-20921..-20929`. Alcance: **Valorización + Niveles + Rotación**. **GOTCHAS CRÍTICOS:** (1) `STOCK_PRODUCTO.CANTIDAD` es el on-hand autoritativo — el kardex `MOVIMIENTOS_STOCK` **NO reconcilia la apertura** (el stock inicial se cargó sin movimiento) → nunca sumar movimientos desde cero; un saldo corrido se ancla al stock actual. (2) Stock es **snapshot** (sin historia) → niveles/valorización a-hoy; el tiempo solo vive en `MOVIMIENTOS_STOCK`. (3) `MOVIMIENTOS_STOCK.TIPO_MOVIMIENTO` viene con **mayúscula mixta** (`ENTRADA`/`Entrada`) → usar `UPPER()`. (4) **No hay lotes/caducidad de mercadería** → alertas de vencimiento NO factibles (`V_ALERTAS_CADUCIDAD_PP` es vencimiento del acuerdo de precio de proveedor, no de la mercadería). **Costo:** `COALESCE(FN_COSTO_PONDERADO(prod, ventana amplia 3650), último precio ACTIVO de PRODUCTO_PROVEEDORES)`; `FN_COSTO_PONDERADO` mira **facturas de compra** (`DETALLE_COMPROBANTE_PROV` estado `'C'`), NO la lista de precios. **Seed demo** (`db/F23_seed_inventario_demo.sql`): puebla mín/máx faltantes + agrega 4 productos demo (Notebook Lenovo/Laptops, Samsung A54/Smartphones, JBL Charge 5/Audio, Smart TV LG/Televisores) + precios de proveedor (Nissei S.A. id 101) — **sin tocar stock** (los precios entran directo a `PRODUCTO_PROVEEDORES`, no por compras que dispararían ENTRADA vía `TRG_MOV_STOCK_DETALLE_PROV`). Sin tabla de metas (la "meta" de inventario son los mín/máx). **Vistas** `V_INV_STOCK`/`V_INV_MOV`/`V_INV_FLUJO_MES`/`V_INV_ROTACION`/`V_INV_CONTEO_DIF` (`db/F23_1_vistas_inventario.sql`) = único source of truth. **Dimensiones:** oficina vía `STOCK_PRODUCTO.ID_OFICINA`→`OFICINAS.CODIGO_OFICINA`; categoría/marca; tiempo (solo movimientos); proveedor (compras). **Páginas:** **P142** Dashboard de Inventario (KPIs + 6 JET charts: valor por categoría, stock vs mín/máx, entradas/salidas por mes, rotación, valor por sucursal donut, top productos + selector de sucursal); **P143** "Generador de Informe de Inventario" (imprimible por filtros: sucursal + categoría + rango → `FN_INFORME_INVENTARIO_HTML` en `db/F23_2_informe_inventario_html.sql`, barras CSS + `@media print`, estilo `kude` azul; snapshot + rango para flujo). El botón de P142 abre P143. Import **aislado** (`install_p142/143.sql`); el menú lo agrega el PO. Datos de los 4 productos nuevos + valorización = **demo** (presentar así en la defensa).
- `PLAN_CUENTAS_PAGAR.md` — **Cuentas por Pagar → Orden de Pago (F24)**, feature **transaccional** que genera la deuda a proveedores y permite pagarla; **prerequisito de F25** (aging de deuda). Cerrado el 2026-07-02. Errores `-20935..-20939`. **Decisiones PO:** (1) **solo crédito** genera CxP → columna `COMPROBANTES_PROVEEDOR.FORMA_PAGO` (`'1'`=crédito/`'21'`=contado, default contado); (2) vencimiento = `FECHA_EMISION + PROVEEDORES.PLAZO_PAGO_DIAS` (nueva col, default 30); (3) **OP en dos pasos**: `PRC_GENERAR_ORDEN_PAGO` emite `BORRADOR` (autoriza, **NO baja saldo**) y `PRC_CONFIRMAR_ORDEN_PAGO` ejecuta el pago (baja `SALDO`, marca CxP `PAGADA`/`PARCIAL`, `SELECT FOR UPDATE`); (4) **sin impacto en caja** (fuera de alcance, `MOVIMIENTOS_CAJA.ID_CLIENTE`/`ID_CAJA` son NOT NULL). **`CUENTAS_PAGAR` se puebla con `TRG_INS_CUENTAS_PAGAR`** (`AFTER INSERT OR UPDATE ON COMPROBANTES_PROVEEDOR`, espejo de `TRG_INS_CUENTAS_COBRAR` pero **pago único** sin cuotas; la deuda nace **al registrar** la factura `ESTADO='R'`, no espera recepción; guard `NOT EXISTS`). Nueva col `CUENTAS_PAGAR.FECHA_VENCIMIENTO`. Tablas `ORDENES_PAGO`/`ORDEN_PAGO_DET` (reusan la secuencia huérfana `SEQ_ORDEN_PAGO_ID`). Vista `V_CXP_DEUDA` (deuda + días de atraso con `FN_HOY`) = source de P146/P147. **DB:** `db/F24_cuentas_pagar.sql` (DDL+trigger), `F24_1_ordenes_pago.sql` (tablas+procs `PRC_GENERAR/CONFIRMAR/ANULAR_ORDEN_PAGO`), `F24_2_backfill_cxp.sql` (7 CxP históricas, todas asumidas crédito), `F24_3_vista_cxp.sql`, `F24_4_orden_pago_html.sql` (`FN_ORDEN_PAGO_HTML`, doc estilo `kude` **no SIFEN**). **Páginas:** **P146** Deuda a Proveedores (IR), **P147** Generar OP (Classic Report con `APEX_ITEM.TEXT` de monto por fila + proceso que arma `SYS.ODCINUMBERLIST` y llama `PRC_GENERAR`), **P148** Órdenes de Pago (**IR**; detail-link "Resolver"→P150 y columna N°→P149, patrón P124/P126), **P149** Documento OP (imprimible), **P150** "Resolver Orden de Pago" (**modal** estilo P126: muestra detalle de comprobantes aplicados + botones Confirmar (elige `METODOS_PAGO`) / Anular (pide motivo, guardado en `ORDENES_PAGO.MOTIVO_ANULACION`); botones condicionados a `ESTADO='BORRADOR'`; DA refresca P148 al cerrar). `PRC_ANULAR_ORDEN_PAGO` toma `p_motivo`. **Ediciones:** P42 (ítem `PLAZO_PAGO_DIAS`), P41 (columna IR), P70 (selector "Condición"→`FORMA_PAGO`). Import **aislado**; menú lo agrega el PO. **Gotcha APEX 24.2.17:** en `create_report_columns` (Classic Report) el formato es `p_column_format`, **no** `p_format_mask` (ese es de IR/worksheet). **Add-on (2026-07-02):** **P97** "Factura de Proveedor Documento" remaquetada al estilo `kude` vía `FN_KUDE_FACTURA_PROV_HTML` (`db/kude_factura_proveedor.sql`, espeja F12 pero **del lado compra**: emisor=PROVEEDOR, receptor=nuestra EMPRESA desde `PARAMETROS` EMPRESA; muestra condición Contado/Crédito y vencimiento de la CxP; **desglosa IVA** derivándolo del producto (`PRODUCTOS.ID_TIPO_IVA`→`TIPO_IVA.PORCENTAJE` 10/5, precio IVA incluido → 5%=base·5/105, 10%=base·10/110), ya que `DETALLE_COMPROBANTE_PROV` no guarda IVA por línea; registro interno "sin validez fiscal"). Reemplazó el HTML inline viejo con emisor hardcodeado.

## Environment

Configured in `.claude/settings.json`:

- `SQLCL_CONNECTION=tesis_db` — named SQLcl connection used by the APEX/SQLcl skills.
- `APEX_APP_ID=100`
- `APEX_WORKSPACE=WKSP_WORKPLACE`

Credentials live in `wallet/` (Oracle Cloud wallet, gitignored). The DB is the Autonomous Database at `GD48788B1691042-ORAPDBTES.adb.sa-vinhedo-1.oraclecloudapps.com` and APEX is reached at `/ords/apex` on that host. The SSL wallet expires **2031-03-26** — re-download before then.

## Two parallel APEX trees

The repo intentionally keeps two separate exports of the same app side-by-side. Know which one you are editing:

- **`apex-learn/f100/`** — *Read-only reference.* Full export of the live app, including `create_application.sql`, `delete_application.sql`, every shared component, all pages, files, plugin settings, etc. Treat this as the baseline / source of truth for what production currently looks like. Do not edit by hand; refresh it by re-exporting.
- **`apex-work/f100/`** — *Patch staging area.* A pared-down tree that is built up file-by-file as we change things, then imported selectively via the two install scripts in its root:
  - `install_page.sql` — imports only the pages currently listed inside it (each page paired with its `delete_NNNNN.sql`). Edit this file to control which pages get pushed.
  - `install_component.sql` — imports only the shared components currently listed (today: `navigation_menu`).
  Both scripts wrap their work between `application/set_environment.sql` and `application/end_environment.sql`, which call `wwv_flow_imp.import_begin` / `import_end` with the workspace + app ID + offset. Never bypass them.

When adding a page or component to the work tree, you must also add the matching `@@…` line(s) to the relevant install script — exports placed in `apex-work/` are dead weight until referenced there.

Other directories:
- `db/` — versioned SQL migrations (DDL/DML/PLSQL outside APEX). One file per feature: `db/F4_estados.sql`, etc. Each script must be idempotent and end with a verification block. See `db/README.md`. Apply via `sql -S -name $SQLCL_CONNECTION @db/<file>.sql`.
- `export-prev/` — older full exports kept as historical backups (includes a large `f100.sql`, zips, and `readable/`). Ignore unless explicitly asked.
- `apexlang/` — empty placeholder.

## Common workflow

1. Use the `apex-export` / `apex-describe` skill to pull the current state of a component out of the live app into `apex-work/f100/application/...`.
2. Edit the exported `.sql` (page, region, shared component) in `apex-work/`.
3. Add it (and its `delete_NNNNN.sql` pair, for pages) to `install_page.sql` or `install_component.sql`.
4. Use the `apex` / `sqlcl` skill (which goes through SQLcl MCP using `SQLCL_CONNECTION=tesis_db`) to run the install script against the workspace.
5. Verify in the live APEX app.

Commit messages follow Conventional Commits in Spanish (`feat:`, `fix:`, `style:`, `cleanup:` …) — match the existing style.

## Skills to prefer

This repo is set up for the APEX toolchain. For anything touching the application, prefer these over hand-rolled SQLcl commands:

- `apex-export`, `apex-describe`, `apex-learn` — read-only exploration of the live app.
- `apex` — exporting/patching/importing components (pages, regions, items, processes, DAs, validations, LOVs, IR/IG, auth schemes, templates, etc.).
- `sqlcl` — direct SQL/PLSQL, schema inspection, Liquibase, Data Pump.

## Pitfalls specific to this repo

- **Fecha/hora: la BD corre en UTC (`DBTIMEZONE=+00:00`).** `SYSDATE` y `SYSTIMESTAMP` están en UTC → después de ~21hs locales adelantan el día. **NUNCA** uses `SYSDATE`/`SYSTIMESTAMP` para fecha/hora que se **guarde o se muestre** (negocio **y** auditoría): usá `WKSP_WORKPLACE.FN_HOY` (fecha) o `WKSP_WORKPLACE.FN_AHORA` (fecha+hora), ambas en `db/F19_fecha_local.sql` y basadas en `America/Argentina/Buenos_Aires` (UTC-3). **No uses `America/Asuncion`** (timezone file de la BD desactualizado → devuelve UTC-4 en invierno). Únicas excepciones en UTC: tokens de `PKG_EMPLEADOS` (auth), DEFAULTs de columna (Oracle no permite función de usuario en DEFAULT → respaldar con trigger que setee `FN_AHORA`), y DMLs de migración de una vez. Guía completa con tabla de casos: `GUIA_FECHA_HORA.md`. El fix vive en `db/F19`/`F20`/`F21` + ediciones in-place + `apex-work/f100/install_p67.sql`, `install_p_tz.sql` e `install_p_tz_aud.sql`.

- **El módulo Reverso de Cobro (F15) está OCULTO a propósito** (no es un bug). Interruptor maestro `PARAMETROS.REVERSO_COBRO_ACTIVO='N'` (`db/F15_2_ocultar_reverso.sql`). El backend (`PRC_*_REVERSO_COBRO`, `FN_COBRO_REVERSABLE`, `V_SOLICITUDES_REVERSO`, tablas) y las páginas P128/P129/P130 **siguen existiendo**, pero el ícono "Solicitar reverso" de P99 (guard en el SQL de `REVERSO_HTML`) y la entrada de menú "Reversos de Cobro"→P129 se ocultan cuando el parámetro ≠ `'S'`. Razón: la NC ya reconcilia las cuotas pendientes y el reverso devuelve efectivo (contra la regla del PO "no se devuelve dinero"). **Reactivar:** `UPDATE PARAMETROS SET VALOR_TEXTO='S' WHERE CLAVE='REVERSO_COBRO_ACTIVO'` + re-mostrar la entrada de menú en el Builder. NO "arreglar" el ícono/menú faltante. Ver `PLAN_REVERSO_COBRO.md §12`.
- The `delete_NNNNN.sql` file for a page is **required** before its `page_NNNNN.sql` — APEX import needs the page dropped first. Untracked `page_*.sql` files in `apex-work/.../pages/` without a matching `delete_*.sql` indicate an in-progress export.
- `wallet/`, `*.env`, and `export-prev/festive-varahamihira-3893ce/` are gitignored — do not stage them.
- Page exports embed JavaScript with Spanish identifiers and `unistr(...)` escapes for accented characters. When editing, preserve the `wwv_flow_string.join(wwv_flow_t_varchar2(...))` chunking and the `unistr('...\00EDsmbolo...')` form; do not "fix" them to plain strings.
- The export header pins `p_version_yyyy_mm_dd=>'2024.11.30'`, `p_release=>'24.2.17'` (Oracle aplicó patch 24.2.15→24.2.17 al ATP el 2026-06-06), `p_default_workspace_id=>7697821598969118`, `p_default_application_id=>100`. If a re-export changes these, the workspace was upgraded — flag it before importing.
- **Renombrado “Orden de Venta” → “Presupuesto/Pedido” es solo UI.** Las tablas `ORDENES_VENTA` y `DETALLE_ORDEN` se conservan con sus nombres originales y todas sus columnas (`ID_ORDEN`, `FECHA_ORDEN`, etc.). El cambio aplica únicamente a `p_name`, `p_step_title`, `p_plug_name`, labels y textos visibles de las páginas P52/P54/P6 y al menú. No renombrar objetos de BD ni columnas como parte de este renombrado. Ver `PLAN_VENTAS.md` Feature 1.
- **Renombre de tabla aplicado en F8**: `RECIBOS_COBRO` ahora es `MOVIMIENTOS_CAJA` (y `DETALLE_RECIBO_COBRO` es `DETALLE_MOVIMIENTO_CAJA`). Se agregaron columnas `TIPO`, `ID_COMPROBANTE`, `USUARIO`, y campos del documento recibo (`NRO_RECIBO`, `ID_TALONARIO_RECIBO`, `FECHA_EMISION_RECIBO`, `ID_CUENTA_COBRAR_DET`). La PK pasó de `ID_RECIBO` a `ID_MOVIMIENTO`. Ver `PLAN_FACTURACION.md` §4 F8.A.
- **`IMAGE_PREFIX` apunta a CDN público de Oracle** (`https://static.oracle.com/cdn/apex/24.2.17/`), no a `/i/`. Es un workaround del 2026-06-07 porque el ORDS embebido del ATP no expone `/i/` post-upgrade. Si Oracle alguna vez arregla el deploy, revertir con `apex_instance_admin.set_parameter('IMAGE_PREFIX', '/i/')`. NO es algo que rompimos nosotros — fue side-effect del patch APEX 24.2.15→24.2.17.
- **Regla "1 caja abierta por empleado"** (no "1 caja por día"). El trigger `TRG_CAJA_UNA_POR_DIA` (nombre engañoso, mantengo por historicidad) bloquea apertura solo si el empleado ya tiene una caja con `ESTADO='A'`. Se pueden abrir múltiples cajas del mismo día si las anteriores están cerradas. La factura sí valida que la caja sea del día actual.
- **El export APEX (`apex export -split`) sobreescribe `install_page.sql`** dejando solo la página exportada. Si vas a re-exportar, restaurá el `install_page.sql` completo después (los demás @@ de páginas previas se pierden si no se restauran).
- **`TALONARIOS.ID_OFICINA` y `TALONARIOS.ESTABLECIMIENTO` son columnas derivadas desde F10/F10.1** (commit `f10-talonarios-set`). La FK regulatoria es `ID_CAJA_CONF` → `CAJA_CONF`; el trigger `TRG_TALONARIO_DERIVA_OFICINA` (BEFORE INSERT/UPDATE OF ID_CAJA_CONF) setea `ID_OFICINA` desde `CAJA_CONF.ID_OFICINA` y `ESTABLECIMIENTO` desde `OFICINAS.ESTABLECIMIENTO_SET`. NO setear `ID_OFICINA` ni `ESTABLECIMIENTO` a mano al insertar talonarios — el trigger los sobreescribe. Si `OFICINAS.ESTABLECIMIENTO_SET` es NULL para la oficina afectada, el INSERT falla con `-20961` (cargar primero el código SET de la sucursal). Restricción SET: 1 talonario activo por `(ID_CAJA_CONF, TIPO_COMPROBANTE)` (índice `UQ_TALONARIO_CAJA_TIPO_ACT`). Helper de sesión: `FN_CAJA_CONF_USUARIO(:APP_USER)` (análogo a `FN_OFICINA_USUARIO_V2`). Ver `PLAN_TALONARIOS_SET.md`.
